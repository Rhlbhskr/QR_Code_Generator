# 🧾 QR Code Generator (Python)

A simple Python script that generates a QR code from any text or URL and saves it as an image file.

## 📦 Requirements
Before running the script, make sure you have the **`qrcode`** library installed.

```bash
pip install qrcode[pil]
